{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build:nest": {
      "outputs": ["dist"],
      "inputs": ["package.json", "src/**"]
    },
    "dev:nest": {
      "cache": false,
      "persistent": true
    }
  }
}